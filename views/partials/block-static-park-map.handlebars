<div class="block-static-park-map">
  <div class="big-park-map">
  </div>
  {{#unless hideLocatorMap}}
  <div class="small-park-map">
  </div>
  {{/unless}}
  {{#unless hideDirections}}<button class="action directions-action active">{{#__}}Directions{{/__}}</button>{{/unless}}
</div>
