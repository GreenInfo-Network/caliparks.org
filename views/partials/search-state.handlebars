{{#if story}}
  {{story}}
{{else}}
  {{#if query.bbox}}
    {{#if startat}}
    {{total}} {{#__}}more parks in this area {{/__}}
    {{else}}
    {{#__}}{{total}} parks in this area {{/__}}
    {{/if}}
  {{else}}

    {{#if query.with}}
      {{total}} parks with {{#formatActivityList}}{{query.with}}{{/formatActivityList}}

    {{else}}
    {{#if place}}
      {{#if parks}}

      {{#if query.near}}
      {{#if startat}}
      {{total}} {{#__}}more parks near {{/__}}
      {{else}}
      {{#__}}The {{total}} parks nearest to {{/__}}
      {{/if}}

      {{#if homeLocation}}
      {{#__}}you{{/__}}
      {{else}}
      {{place.name}}
      {{/if}}
      {{/if}}
    {{else}}

      {{#__}}No parks found near{{/__}} “{{place.name}}”{{#__}}; try expanding your filter.{{/__}}

      {{/if}}
      {{else}}

      {{#if query.near}}
      “{{query.near}}” {{#__}}could not be found.{{/__}}
      {{/if}}

      {{/if}}
      {{/if}}
    {{/if}}

{{/if}}
