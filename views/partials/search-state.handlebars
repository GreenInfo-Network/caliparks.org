{{#if story}}
  {{story}}
{{else}}
  {{#if query.bbox}}
    {{#if startat}}
    {{#__ arg=total}}%s more {{#pluralize total}}park|parks{{/pluralize}} in this area {{/__}}
    {{else}}
    {{#__ arg=total}}%s {{#pluralize total}}park|parks{{/pluralize}} in this area {{/__}}
    {{/if}}
  {{else}}

    {{#if query.with}}
      {{#__ arg=total}}%s {{#pluralize total}}park|parks{{/pluralize}} with{{/__}} {{#formatActivityList}}{{query.with}}{{/formatActivityList}}

    {{else}}
    {{#if place}}
      {{#if parks}}

      {{#if query.near}}
      {{#if startat}}
      {{#__ arg=total}} %s more {{#pluralize total}}park|parks{{/pluralize}} near {{/__}}
      {{else}}
      {{#__ arg=total}}The %s {{#pluralize total}}park|parks{{/pluralize}} nearest to {{/__}}
      {{/if}}

      {{#if homeLocation}}
      {{#__}}you{{/__}}
      {{else}}
      {{place.name}}
      {{/if}}
      {{/if}}
    {{else}}

      {{#__ arg=place.name}}No parks found near “%s”; try expanding your filter.{{/__}}

      {{/if}}
      {{else}}

      {{#if query.near}}
       {{#__ arg=query.near}}“%s” could not be found.{{/__}}
      {{/if}}

      {{/if}}
      {{/if}}
    {{/if}}

{{/if}}
